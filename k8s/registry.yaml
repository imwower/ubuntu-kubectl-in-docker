apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: registry
  namespace: base
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: registry.web
    http:
      paths:
      - path: /
        backend:
          serviceName: registry
          servicePort: 5000